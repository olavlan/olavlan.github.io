[project]
name = "site"
version = "0.1.0"
description = "My personal website, built with zensical"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["zensical>=0.0.11"]

[tool.uv.workspace]
members = [
    "prototypes/publish_quarto",
]
